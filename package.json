{
  "name": "library-management-service",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix library-management-server && npm install --prefix library-management && npm run build --prefix library-management"
  }
}